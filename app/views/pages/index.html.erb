<% if false %>
<div class="news">
    <ul class="list-unstyled">
      <% @events.each do |event| %>
        <li><%= event %></li>
        <p class="separator">----------------</p>
      <% end %>
    </ul>
</div>
<% end %>


<div class="container">
  <div class="row">
    <div class="">
      <div class="news">
          <ul class="list-unstyled">
            <% @events.each do |event| %>
              <li><%= event %></li>
              <p class="separator"><%= "-" * 100 %></p>
            <% end %>
          </ul>
      </div>
    </div>
  </div>
</div>
